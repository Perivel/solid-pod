import { SSRType } from '../../config/config.well';

/**
 * RenderOptions
 * 
 * Specifies Rendering options.
 */

export interface RenderOptions {

    /**
     * The SSR mode to use
     */
    ssrMode: SSRType
}
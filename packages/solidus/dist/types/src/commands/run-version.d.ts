import { CommandType } from './../utilities/index';
/**
 * runVersion()
 *
 * Executes the version command.
 * @returns CommandStatus that represents the command status.
 */
export declare const runVersion: CommandType;

/**
 * server/http-server.interface.ts
 * 
 * server/http-server.ts defines the interface for an http server.
 */

 export interface HttpServerInterface {

    /**
     * start()
     * 
     * start() starts the HttpServer.
     */
    
    start(): void;
}
{
  "name": "solidus-js",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "release": "lerna publish --no-private",
    "build": "lerna run build",
    "bootstrap": "lerna bootstrap"
  }
}

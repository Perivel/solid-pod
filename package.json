{
  "name": "solidus-js",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "publish": "lerna publish --no-private",
    "build": "lerna run build"
  }
}
